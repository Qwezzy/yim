# YIM website (2026 rebuild)

Static site. No Bootstrap. No jQuery.

Donations: PayFast sandbox in `js/config.js`. Switch `sandbox: false` and add live merchant details to take real payments.

Azure Static Web Apps deploys from `main` using the existing workflow. Vercel project: `yim`.
